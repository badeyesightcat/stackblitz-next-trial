import { LayoutProps } from "@/types/common";
import React from "react";

const ProfileItemLayout = (props: LayoutProps) => {
  // console.log(props);
  return <div>this is ProfileItemLayout</div>;
};

export default ProfileItemLayout;
